<Window x:Class="WindowsOcrApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Screen Snip OCR" Height="400" Width="640" MinHeight="320" MinWidth="480">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button x:Name="SnipButton"
                Content="Capture Screen Snip"
                Height="42"
                Width="200"
                HorizontalAlignment="Left"
                Click="SnipButton_Click" />

        <TextBox Grid.Row="2"
                 x:Name="OcrResultTextBox"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="14"
                 Text="Click 'Capture Screen Snip' to select a region and extract text." />
    </Grid>
</Window>
