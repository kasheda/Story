<Window x:Class="WindowsOcrApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Windows OCR" Height="600" Width="900" MinHeight="480" MinWidth="720">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Spacing="10">
            <Button x:Name="OpenImageButton" Content="Open Image" Width="110" Click="OpenImageButton_Click" />
            <Button x:Name="SnipButton" Content="Capture Snip" Width="110" Click="SnipButton_Click" />
            <Button x:Name="RunOcrButton" Content="Run OCR" Width="110" Click="RunOcrButton_Click" />
            <TextBlock VerticalAlignment="Center" Margin="20,0,0,0" Text="Tessdata Path:" />
            <TextBox x:Name="TessdataPathTextBox" Width="220" Margin="5,0,0,0" />
            <Button x:Name="BrowseTessdataButton" Content="Browse" Width="70" Click="BrowseTessdataButton_Click" />
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="#CCC" BorderThickness="1" CornerRadius="4" Margin="0,12,0,12">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Image x:Name="PreviewImage" Stretch="Uniform" />
            </ScrollViewer>
        </Border>

        <DockPanel Grid.Row="2">
            <TextBlock DockPanel.Dock="Top" FontWeight="Bold" Margin="0,0,0,6" Text="OCR Output" />
            <TextBox x:Name="OcrResultTextBox" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />
        </DockPanel>
    </Grid>
</Window>
